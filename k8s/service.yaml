apiVersion: v1
kind: Service
metadata:
  name: multi-agent-research
  namespace: multi-agent-research
spec:
  type: NodePort

  selector:
    app: multi-agent-research

  ports:
  - port: 80
    targetPort: 8501
    nodePort: 30080